{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Lets the Quit button end the process. Desktop only -- mobile hides that button and doesn't build the plugin.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["process:allow-exit"]
}
